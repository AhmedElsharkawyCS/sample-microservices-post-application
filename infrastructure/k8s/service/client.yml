apiVersion: v1
kind: Service
metadata:
  name: client-app-service
spec:
  type: NodePort
  selector:
    app: client-app
  ports:
    - name: client-app
      protocol: TCP
      port: 8000
      targetPort: 8000
